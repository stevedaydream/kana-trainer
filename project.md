# project.md — 五十音練習器 完整版

## 專案目標

把現有單檔原型 `kana-trainer-v2.html` 重構為「程式與題庫分離」的完整 10 級五十音練習器。題庫與圖片由 Gemini 產出（prompt 見 `gemini-kana-databank-prompt.md`），Claude Code 負責重構、整合、驗證、補齊程式功能。

## 已確定的產品決策（經逐項討論確認，不得擅自更改）

1. **核心迴圈＝翻牌提取練習**：亮假名 → 使用者先唸 → 點卡揭曉（羅馬音＋字源＋圖＋單詞＋播音）→ 自評「✓記得 / ✗忘了」。
2. **錯題加權**：✗ → 出現機率 ×4（上限 16）；✓ 一次權重減半、連對兩次歸 1。畫面提供「弱項榜」。考試答錯同樣餵入權重。
3. **三層記憶錨**：字源聯想（主）＋ 單詞（輔）＋ 圖片。圖片載入優先序：`img/{key}.png` → 內建 SVG → emoji。
4. **考試（每級 Part 4，60 秒）三題型隨機**：聽選（播音選假名）、讀選（亮假名選羅馬音）、平片配對（あ↔ア 雙向）。答錯播正確音。
5. **熱身可選**：開始前可勾選「每字播放跟讀一輪」，可跳過。
6. **級別結構**：第一階段 10 級（清音 あ行～わ行），每級 Part1 平假名 / Part2 片假名 / Part3 混合 / Part4 考試。**第二階段延伸 11～14 級**（濁音/半濁音/拗音，規格見文末「第二階段」章節），同一條解鎖線。
7. **無語音環境降級**：偵測不到 speechSynthesis 時靜音運作、考試剔除聽選、顯示提示，絕不可拋錯。

## 目標檔案結構

```
kana-trainer/
├─ index.html          # 主程式（UI + 邏輯，不含任何題庫資料）
├─ data/
│  └─ levels.js        # 題庫外掛檔（唯一資料來源）
├─ img/                # 配圖，檔名 = 題庫 img 鍵 + .png（缺檔自動退回）
├─ scripts/
│  └─ validate.mjs     # 題庫驗證腳本（node scripts/validate.mjs）
└─ project.md
```

### data/levels.js 介面契約（外掛規格）

不用 JSON + fetch（file:// 開啟會被 CORS 擋），用全域變數：

```js
// data/levels.js
window.KANA_DATA = {
  levels: [
    { id: 1, name: "第一級 あ行", hira: [/*…*/], kata: [/*…*/] },
    // … id 2~10
  ]
};
```

每筆假名物件欄位（與 Gemini prompt 一致）：
`k, r, type("hira"|"kata"), origin, w, zh, img, emoji, pair`

`index.html` 只透過 `window.KANA_DATA` 取資料；**換題庫＝只換這一個檔案**，主程式零修改。找不到 `KANA_DATA` 時顯示「題庫未載入」而非崩潰。

## 任務清單（依序執行）

### T1 重構拆分
- 從 `kana-trainer-v2.html` 抽出第一級資料 → 建立 `data/levels.js`（先只含第 1 級）。
- 主程式改讀 `window.KANA_DATA`，移除內嵌陣列。
- 內建 SVG 保留在主程式（屬於程式資產，非題庫）；新增級別缺 SVG 時退 emoji 即可。

### T2 程式泛化（原型只支援 あ行，必須修）
- **讀選題選項池**：目前寫死 `["a","i","u","e","o"]`，改為「從當前級別＋已解鎖級別的羅馬音動態抽 4 個（含正解）」。
- **加權統計 key**：weights/streak 改為依 `levels.js` 動態初始化，新假名自動補 1。
- **儲存層**：偵測順序 `window.storage`（Claude 環境）→ `localStorage`（本機瀏覽器，包 try/catch）→ 記憶體。儲存內容：weights、streak、各級最佳成績、解鎖進度。

### T3 解鎖機制
- 第 1 級預設開放；第 N 級考試答對 ≥ 12 題 → 解鎖第 N+1 級。
- 首頁級別列表顯示：🔒 / 已解鎖 / 各級最佳成績。
- 提供隱藏後門 `?unlock=all`（URL 參數）供測試。

### T4 與 Gemini 的分工整合
1. 把 `gemini-kana-databank-prompt.md` 交給 Gemini，取回：18 個陣列（HIRA_2~KATA_10）＋ 81 張配圖清單。
2. **不要直接信任 Gemini 輸出**：先把資料貼入 `data/levels.js`，跑 T5 驗證腳本，逐項修正到全綠。
3. 圖片放入 `img/`，檔名必須等於題庫 `img` 鍵；寫一個快速檢查列出「題庫有鍵但缺圖檔」清單（缺圖可上線，會自動退回）。

### T5 驗證腳本 scripts/validate.mjs（必須全部通過）
- 欄位齊全、型別正確；`type` 只能 hira/kata。
- 羅馬音特例：し=shi、ち=chi、つ=tsu、ふ=fu、を=o、ん=n。
- `pair` 雙向對稱（か.pair==="カ" 且 カ.pair==="か"）。
- `img` 鍵全域唯一；`w` 以該假名開頭（ヲ、ン 依 prompt 允許例外，白名單處理）。
- 每級假名數量與行對應正確（や行 3、わ行 3，其餘 5）。
- 輸出：PASS/FAIL 報告，FAIL 列出筆數與原因。

### T6 驗收清單
- [ ] 直接雙擊 `index.html`（file://）可完整運作、有聲。
- [ ] 刪除 `data/levels.js` → 顯示友善錯誤，不白屏。
- [ ] 無 speechSynthesis 環境（可用 `?notts=1` 模擬）→ 靜音降級、考試無聽選。
- [ ] 刪光 `img/` → 全部退回 SVG/emoji，無破圖。
- [ ] 第 1 級考 12 題 → 第 2 級解鎖且重開瀏覽器仍保留。
- [ ] validate.mjs 全綠。

## 風格約束
- 純 HTML/CSS/Vanilla JS，零框架、零建置步驟、零外部 CDN。
- 沿用現有視覺：習字帳田字格卡片、瑠璃藍/朱色/墨色配色。
- 行動裝置優先（max-width 520px 版心）。

## 第二階段：進階發音 11～14 級（先預留，10 級驗收後才動工）

### 級別規劃
| 級別 | 內容 | 音數 |
|---|---|---|
| 11 | 濁音 が・ざ・だ 行（がぎぐげご ざじずぜぞ だぢづでど） | 15 |
| 12 | 濁音 ば 行 ＋ 半濁音 ぱ 行（ばびぶべぼ ぱぴぷぺぽ） | 10 |
| 13 | 拗音 I（きゃ しゃ ちゃ にゃ ひゃ 系，各 ゃゅょ） | 15 |
| 14 | 拗音 II（みゃ りゃ ぎゃ じゃ びゃ ぴゃ 系） | 18 |

### 第一階段就要預留的相容性（T1/T2 執行時一併處理）
1. **`k` 欄位可為多字**：拗音如 `"きゃ"` 是一筆資料。卡片與考試選項字級需自適應（1 字 104px、2 字約 64px）。
2. **羅馬音驗證表可擴充**：validate.mjs 的特例表設計成資料驅動（じ=ji、ぢ=ji、づ=zu、しゃ=sha、ちゃ=cha、じゃ=ja…），不要寫死在邏輯裡。
3. **`origin` 欄位允許改為規則說明**：濁音/拗音沒有獨立字源，格式改為「か ＋ 濁點（゛）→ が」「き ＋ 小さい や → きゃ」。
4. **配對題泛化**：pair 機制不變（が↔ガ、きゃ↔キャ）。
5. 解鎖門檻沿用：前一級考試 ≥ 12 題。

### 第二階段的 Gemini 分工
屆時另寫 `gemini-kana-databank-prompt-phase2.md`：濁音/拗音共 58 音 ×2（平/片）的單詞、翻譯、配圖清單；字源欄改為上述規則說明格式。

